# common aliases

# alias database

alias l="ls -al"
alias grep="grep --color=auto"

alias g="git status"
alias gd="git diff"
alias gdd="git diff --cached"
alias gsu="git submodule update --recursive --init"
