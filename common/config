# misc config

case "$(uname -s)" in
    Linux*)
        . ~/.dotfiles/common/config.linux
        ;;
    Darwin*)
        . ~/.dotfiles/common/config.darwin
        ;;
esac

. ~/.dotfiles/common/config.common
