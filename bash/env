# environment variables

case "$(uname -s)" in
    Linux*)
        . ~/.dotfiles/bash/env.linux
        ;;
    Darwin*)
        . ~/.dotfiles/bash/env.darwin
        ;;
esac

. ~/.dotfiles/bash/env.common
