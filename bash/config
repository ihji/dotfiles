# misc config

case "$(uname -s)" in
    Linux*)
        . ~/.dotfiles/bash/config.linux
        ;;
    Darwin*)
        . ~/.dotfiles/bash/config.darwin
        ;;
esac

. ~/.dotfiles/bash/config.common
