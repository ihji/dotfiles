# alias

case "$(uname -s)" in
    Linux*)
        . ~/.dotfiles/bash/aliases.linux
        ;;
    Darwin*)
        . ~/.dotfiles/bash/aliases.darwin
        ;;
esac

. ~/.dotfiles/bash/aliases.common
